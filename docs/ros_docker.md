# シミュレーション
- rootユーザーで行ってください。

- シミュレーション実験用のROSパッケージをインストールする。
  ```
  $ sudo apt update
  $ sudo apt -y install ros-$(rosversion -d)-turtlesim
  ```
- 3つの端末を
- 環境変数などを設定する。
  ```
  $ source ros_entrypoint.sh
  ```
